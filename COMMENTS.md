Comment Style Guide:

Each code file should have an extended comment at the top explaining what the file's purpose is.

Each backend/middleware method should have a brief explanation for its isolated purpose as well as definitions for input and output variables if applicable.

Each frontend file should have sections marked, defining each visual feature with a 'title' comment and grouping features of the same arbitrary category together with comments.
